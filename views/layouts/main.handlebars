<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Doggy Daycare</title>
  <link rel="stylesheet" href="/css/jass.css">
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css">
 <script src="https://kit.fontawesome.com/ea78308f93.js" crossorigin="anonymous"></script>

</head>

<body class="is-family-monospace">
  <div >
    <header class="">
      <nav class="navbar is-primary" role="navigation" aria-label="dropdown navigation">
        <div class="navbar-menu">
          <div class="navbar-start">
              <a class="navbar-item">
                <i class="fa fa-paw fa-3x"></i>
              </a>
            <div class="navbar-item has-text-black is-size-2">
              Doggy Daycare
            </div>
          </div>
            <div class="navbar-item navbar-end is-size-5">
              {{#if logged_in}}
              <a href="/dashboard">profile</a> |<!--I do not think dashboard link goes anywhere, may need to be user?-->
              <button class="no-button" id="logout">logout</button>
              {{else}}
              <a href="/login">login</a>
              {{/if}}
            </div>
        </div>
      </nav>
    </header>
    
    <image class=""></image>

    <main class="">
      {{{ body }}}  <!--uniform design, template, content is what is changing (all the other handlebars) -->
    </main>
  </div>

  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>